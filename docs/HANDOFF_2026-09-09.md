# tw-hold 交接 — 多軌體檢完成，下一步先讓朋友測主架構

> **自足執行指令。你在全新對話、沒有上下文。** 讀完這份就能動工。
> 環境：Windows，`d:\g\claude\` 底下有 `tw-swing\`、`tw-hold\`、`books\`。
> ⚠️ bash 工具 cwd 是 `d:\g\claude`——跑腳本先 `cd /d/g/claude/tw-hold` 或 `tw-swing`。
> ⚠️ 本機 console cp950——print 不要用 emoji；跑 python 一律 `PYTHONIOENCODING=utf-8`。
>
> **本版建立 2026-09-09**，取代 `HANDOFF_2026-09-08.md`（那份 v1 收尾全完成）。

---

## 0. 一分鐘現況

| | commit | 測試 |
| :-- | :-- | :-- |
| tw-hold `main` | `2c52022`（+ 本次 docs commit） | `pytest -q` → **83 passed** |
| tw-swing `master` | `a9a49fd` | 789 passed（本輪沒碰） |

兩 repo clean + push。app：<https://tw-hold-jchm8ooiwp7ewqisfzmpoo.streamlit.app/>
（Streamlit Community Cloud，`tongxiaooppo-boop/tw-hold` main，push 即自動重佈；app 內密碼擋）。

**開工前檢查**：
```
cd /d/g/claude/tw-hold && git status && PYTHONIOENCODING=utf-8 python -m pytest -q
```

---

## 1. 下一步：**先讓朋友測主架構，AI 不急**

使用者裁決（2026-09-09 早）：
- **主架構先做穩、讓朋友測** —— 多軌體檢 + 個股查詢這一塊。
- **AI 解說層不急** —— 方向已想清楚寫進 `docs/AI_LAYER.md §11`，之後照做。
- 個股查詢的「500 外擴充 10 檔」是 backlog（`docs/PLAN.md` Backlog 區），**不做即時查詢**。

所以下一次對話**沒有指定要動的功能**——等使用者回饋（朋友測完）或指定。
若要碰 AI 層 → 讀 `docs/AI_LAYER.md`（整份，尤其 §11）。

---

## 2. 這一輪（2026-09-09 早上）做了什麼

### 多軌體檢分頁（原「三軌體檢」→「多軌體檢」）

回收舊專案 `books/claude/me/taiwan-stock-analyzer-v3` 的四風格判準，**打分全丟、只留檢核**
（照 F-Score 9 分項模式，PRD §4.1／§5.1：不加總、不給 verdict／買價／排名）。

- **NAV 加第 5 分頁「多軌體檢」**：輸入代號 → 四 tab（⚡短線 / 🟠長波段 / 🔵價值 / 🟢定存）
- 每軌：`checklist.{short,swing,value,deposit}_checks(d)` → `[{組,項目,門檻,現值,狀態}]`
  - **價值 / 定存**：讀 `data/derived/factors_{value,deposit}.parquet`（清單頁同一份因子）
  - **短線 / 長波段**：`bundle_data` 原始 df 現算（MACD／CCP／ATR／Minervini 7 條等）
- **UI**：頂端白話摘要（`summarize()`：未達門檻 / 待確認 / 風險命中，**不加總分**）
  + 逐區塊小標 + 迷你表（狀態欄上色、全攤開不內捲）+ 每軌對應圖（時間跨度跟各軌尺度：
  短線近 3 月 / 波段近 1 年 / 價值近 5–6 年 / 定存股利近 12 年）
- **利息保障倍數**：bundle 無利息費用欄 → 使用者裁定「整條不出現」
- **融資融券**：bundle 無 → 短線軌相關條件不出現
- **短線軌警語最強**（`SHORT_DISCLAIMER`）：零回測、tw-hold 是長期工具、短線用 tw-swing

### 表頭改名
`st.title` / `page_title`：`tw-hold` → **`APP_NAME = "持股觀測站"`**（repo 名不動；非投顧語氣）

### 新圖（`app/stockcharts.py`）
`roe_trend` / `balance_health`（負債比+流動比）/ `yield_trend`（現金殖利率）。
個股查詢頁 + 多軌體檢頁都接了。

### 資料管線坑（修好）
`factors_{value,deposit}.parquet` 是 **gitignore 的 build 產物** → Cloud 上沒有 →
價值/定存軌一律「不在因子表」。修：
1. `build_lists.py` 落地 parquet（原本只有 `build_factors.__main__` 寫）
2. `rebuild.yml` 把兩張表推到 tw-hold 的 **`derived-latest` release**（公開 repo 免 PAT）
3. app `_ensure_derived_factors()` 執行期 `urlretrieve`（本地有 build 產物就沿用）
→ **已手動觸發 rebuild.yml 兩次建出 release**，資產在。

### 導覽 / bug
- 個股查詢 ↔ 多軌體檢：**共用代號**（`_stock_input` 用 widget key `_stock_code`
  + 非 widget 鏡像 `_code_mirror`——Streamlit 切頁會清 widget key，鏡像補回）
- 頁尾兩顆同款鈕：`⬆ 回到頂部`（`#top` anchor）｜`切到另一頁`（`?goto=` 由 `_route()` 接手）
- 估值軌主判準從「便宜門檻」改「**估值上緣**」（`valuation_ceiling`）——便宜門檻
  = 5 年均 EPS × PE P30，成長股常年摸不到，當唯一 binary 沒資訊量。降為參考列。

### commits（2c52022 為止，本輪 12 個）
`f512f52` 手機圖表滑動誤觸縮放 → … → `2c52022` 切頁保留代號

---

## 3. 主架構檔案地圖（朋友測 + 你之後動）

| 事 | 檔 |
| :-- | :-- |
| 四軌檢核邏輯 | `app/checklist.py`：`{short,swing,value,deposit}_checks(d)` + `summarize()` + `_G` 分組 |
| app 分頁 | `app/streamlit_app.py`：`_checklist_page`（多軌體檢）、`_stock_page`（個股查詢）、`_render_checks`、`_stock_input` / `_code_mirror`、`_page_footer` / `?goto=`、`_route`、`_factor_row` / `_ensure_derived_factors`、`APP_NAME` |
| 個股資料層 | `app/bundle_data.py`（bundle Release 執行期拉 + pyarrow 按需讀） |
| 圖 | `app/stockcharts.py`（plotly 深色、手機防誤觸縮放、`_style` 共同出口） |
| 因子（價值/定存 parquet 來源） | `build_lists.py`（落地 parquet）、`build_factors.py`（`screen_all`）、`screener/*` |
| 因子表發佈 | `.github/workflows/rebuild.yml`（`derived-latest` release 上傳步驟） |
| 對照表 / 決策 | `docs/ME_CHECKLIST_MAPPING.md` |
| AI 層設計 | `docs/AI_LAYER.md`（**§11 是 2026-09-09 定調**） |
| v1 全貌 | `docs/PLAN.md`（Backlog 區有「500 外擴充 10 檔」） |
| 凍結的因子/估值/分軌 | `PRD.md` |

---

## 4. 持續監控（定期瞄一眼，不是開發）

| 時間 | 事 |
| :-- | :-- |
| ~2026-09-15 | GitHub Actions 額度複查（記憶 `github-actions-quota-check`） |
| 2026-11-14 | 換股日——金融股進定存 holdings，確認換股理由表 |
| 2026-11-30 起 | 換 `tw-hold-bundle-read` PAT（~12/7 到期）；`TWHOLD_DISPATCH_PAT` ~2027-09-08 |
| bundle 累積後 | 6949 沛爾的 `_MANUAL` 分割日期回頭校 |
| 平常 | `publish_bundle` + `rebuild` 有沒有跑綠（失敗發 webhook）；`split_warning` 響了才處理 |

---

## 5. 紅線（一直有效）

- 🔴 **不改 tw-swing `daily.yml` / `rules.yaml` / portfolios / pipeline / 回測**（G-5 時鐘）。
- 🔴 **PRD 凍結**：不改因子設計 / 估值公式 / 分軌。要改記 backlog、v2 再說。
- 🔴 **多軌體檢 / AI**：只打勾 / 只解說——**不加總、不評分、不給 verdict／買價／排名**。
- 🔴 **AI 層**（`docs/AI_LAYER.md`）：傳給 AI 只有規則結果（`evidence_packet`），永不傳原始
  財報 df；新聞/情緒不上欄位不上卡片；AI 輸出 display-only 不回饋規則。
- 🔴 **個股查詢**：只服務 bundle + 因子表（~1000 檔）；**不即時抓單股資料**（PRD §M4）。
  未來擴充上限＝因子表 + 使用者設定的 ~10 檔，就這樣。
- FinMind：token 600/hr、rate ≤ 450、**不 `Throttle.seed()`**、避開週六 `fundamentals.yml`。
- 定存 5% 殖利率硬底線不放水。
- tw-hold 是**公開 repo**：`positions.json` / token / `.env` / **API key** 絕不進版控。

---

## 6. 下一次對話怎麼開

1. 先跑「開工前檢查」（§0）。
2. **沒有使用者新指示 → 不要自己找事做。** 問使用者朋友測的回饋、或等指定。
3. 要動 AI 層 → 讀 `docs/AI_LAYER.md` 整份（§11 是最新定調），**先跟使用者確認要做哪一塊**
   （複製強化 / BYOK 引擎 / 新聞標題列 / 盤前 briefing / MCP），不要一次全做。
4. 要動「500 外擴充 10 檔」→ `docs/PLAN.md` Backlog，作法已寫，**仍不做即時查詢**。
5. 記憶：`tw-hold-checklist-page`、`tw-hold-ai-layer-design`、`tw-hold-plan-v2-decisions`。

# tw-hold 交接 — v1 收尾完成，下一步討論 AI 介入

> **自足執行指令。你在全新對話、沒有上下文。** 讀完這份就能動工。
> 環境：Windows，`d:\g\claude\` 底下有 `tw-swing\`、`tw-hold\`、`books\`。
> ⚠️ bash 工具 cwd 是 `d:\g\claude`——跑腳本先 `cd /d/g/claude/tw-hold` 或 `tw-swing`。
> ⚠️ 本機 console cp950——print 不要用 emoji；跑 python 一律 `PYTHONIOENCODING=utf-8`。
>
> **本版建立 2026-09-08**，取代 `HANDOFF_v1_tail.md`（那份已 v1 全數完成）。

---

## 0. 一分鐘現況

| | commit | 測試 |
| :-- | :-- | :-- |
| tw-hold `main` | `3a662b9` | `pytest -q` → **73 passed** |
| tw-swing `master` | `a9a49fd` | 789 passed |

兩 repo clean + push。app：<https://tw-hold-jchm8ooiwp7ewqisfzmpoo.streamlit.app/>
（Streamlit Community Cloud，`tongxiaooppo-boop/tw-hold` main，push 即自動重佈；app 內密碼擋）。

**🔴 v1 收尾（⑤ UI / ④ 月營收 / ⑥ 小尾巴 / ③ 產業逆風）全部完成。沒有待辦。**
下一步：**跟使用者討論 AI 介入方法**——讀 `docs/AI_LAYER.md`（設計備忘，未實作）。

**開工前檢查**：
```
cd /d/g/claude/tw-hold && git status && PYTHONIOENCODING=utf-8 python -m pytest -q
cd /d/g/claude/tw-swing && git status && PYTHONIOENCODING=utf-8 python scripts/check_daily.py
```

---

## 1. 下一步：AI 介入方法（明天討論）

**設計已寫好** → `docs/AI_LAYER.md`。核心：**AI 只解說規則算出來的東西 + 補質化盲點，
不選股、不給 verdict / 買價 / 排名**（破這條 = `me` 復活，PRD §5.0）。

三層引擎（`reference/llm.py`，未寫）：
1. 本機 CLI headless（`claude -p` / `gemini` / `codex`）——騎訂閱、零 key、零費用，僅本地
2. BYOK API key——使用者自己貼，`st.session_state` only（不落伺服器）
3. 「複製給 AI」（現況）——退路

外部訊息（美國收盤 / 產業趨勢 / 資金流向）：只進「盤前 briefing」+「問 AI」回答，
**不進清單卡片、不進欄位、不進規則**。MCP server 是替代方案（用現有訂閱、對話在 claude.ai）。

參考實作：`books/claude/tw-stock-scanner-main/llm.py`（三層降級）、
`books/claude/me/.../ai/analyzer.py`（v4.0 護欄：只看結果不看原料、explanation 不 decision）。

**使用者要先討論方法、再動手。** 記憶 `tw-hold-ai-layer-design`。

---

## 2. 這個 session（2026-09-08 下半天）做了什麼

### ⑤ UI 收尾
- 清單代號可點 → 跳個股查詢（`st.tabs` 換 `st.radio` session_state 導覽 + `?code=` 連結，
  `_route()` 讀 query param）。
- 股利圖分次配息各段描邊（季配/半年配疊柱看得出分次）。
- 原始季度數據金額欄改「千元 + 千分號」（`_qf_display`）。

### ④ 月營收歷史
- tw-swing `scripts/build_revenue_history.py`：FinMind 快取 → 版控 `revenue_history.parquet`
  （2015~，243k 列）。`build_u1b_bundle._revenue` 改長表 `[ticker, month, revenue, announced]`。
- tw-hold `bundle_data.revenue()` + `stockcharts.monthly_revenue()`（柱=月營收億 + 線=YoY%）+
  個股頁接線。`candidate_pool.revenue_yoy` 加回 `revenue_accel`。

### ⑥ 小尾巴
- **`per.parquet` 每日刷新**：tw-swing `fetch_valuation.py`（TWSE `BWIBBU_ALL` + TPEx，免金鑰）
  → `data/valuation/` 版控快照 → `build_per_parquet.py --from-existing` 併。`publish_bundle.yml`
  每平日跑。記憶 `tw-swing-valuation-daily`。
- **個股頁法人買賣超圖**：`bundle_data.chips()` + `stockcharts.institutional_net()`。
- **PRD §8/§9 更正框**（`tw-data` 公開架構沒實作）+ §10.1 + §11。
- `div_years` 全=12：**使用者裁決接受、不補更早股利**。
- `price_adjuster.py` 移植 / 本地即時補抓：**v1 不做**，列 §4。
- 實驗 B 報告：標過時（`research/backtest_rebalance.py` import `twswing.value.*` 已搬進 tw-hold、
  要先 port；且定存回測本身待重審）。

### ③ 產業逆風旗標
`screener/industry.py`：同產業近 6 月報酬中位數 < −10% 且落後大盤 8pp（≥3 檔才判）→
`industry_headwind` 旗標 + `industry_ret_6m`。**只顯示、不進 verdict**。實測 value 中 3293 鈊象。

### 股票分割還原（額外，因 5904 寶雅冒出「資料不足」追出來）
- `reference/corporate_actions.py`：`SPLITS`（= `corporate_actions_resolved.json` 自動 +
  `_MANUAL` 手動覆寫）+ `IGNORE_JUMPS`。`adjust_per_share` 把事件日前的每股數字 ÷ factor。
- `build_factors.scan_price_jumps()` / `detect_unhandled_splits()`：掃 bundle 跳空。
- **`scripts/resolve_splits.py`（自動）**：`rebuild.yml` 在 build_lists 前跑——偵測跳空 →
  抓 FinMind `TaiwanStockSplitPrice` + `CapitalReductionReferencePrice`（免費、匿名）→
  **FinMind 給 factor、偵測器給日期**（bundle 跳空日）→ 寫 resolved.json。解不了的才發 webhook。
- 現況：resolved.json 有 0052/4747；`_MANUAL` 有 5904/6949；`IGNORE_JUMPS` 4 檔（資料雜訊）。
- `me` 被 2327 國巨的減資逼出 `price_adjuster.py`（跨事件日回測圖崩）——同一個病。
- 記憶 `tw-hold-split-adjustment-gap`。

### 其他
- `bundle_data.ensure_assets()`：本地檔大小 ≠ Release 資產就重抓（schema 換過而容器沒重建的
  bug——2026-09-08 使用者查 4768/2353 看不到月營收，reboot app + 這個修正解掉）。
- **`publish_bundle.yml` 排程 UTC 06:00 → 22:00**（台灣 14:00 → 隔日早上 ~06:00）。
- 上游 data_pack workflow 全文保存 → `tw-swing/docs/reference/upstream_datapack_workflow.yml`。
- GitHub 額度：Free 方案未綁卡 → **不可能被收費**。~9/15 複查用量（記憶 `github-actions-quota-check`）。

---

## 3. 持續監控（不是開發，定期瞄一眼）

| 時間 | 事 |
| :-- | :-- |
| ~2026-09-15 | GitHub Actions 額度複查 |
| 2026-11-14 | 換股日——金融股（2820/2850/2890…）進定存 holdings，確認換股理由表 |
| 2026-11-30 起 | 提醒使用者換 `tw-hold-bundle-read` PAT（~12/7 到期）；`TWHOLD_DISPATCH_PAT` ~2027-09-08 |
| bundle 累積後 | 6949 沛爾生醫的 `_MANUAL` 分割日期回頭校一次 |
| 平常 | `publish_bundle` + `rebuild` 有沒有跑綠（失敗發 webhook）；`split_warning` webhook 響了才處理 |

**分割日後處理**：webhook 響（= FinMind 也沒有的跳空）才要人——加 `corporate_actions.py` 的
`_MANUAL`（真事件、等 FinMind）或 `IGNORE_JUMPS`（資料雜訊）。一般分割全自動。

---

## 4. 日後拓展（有方向、使用者選擇延後 / 未定案）

- **AI 解說層**（§1，`docs/AI_LAYER.md`）——**下一個要討論的**。
- **個股頁「三軌門檻檢視」**：把三軌硬門檻套單一檔顯示 ✓/✗。PRD §4.1「不打分」→ 先定調措辭。
  使用者 2026-09-08 再確認先不做。
- **實驗 B 報告重跑**：先 port `research/backtest_rebalance.py`（`twswing.value.*` → `factors/` /
  `reference/loader.py` / `screener/`）。且定存回測本身待重審（記憶 `tw-hold-backtest-experiments-outcome`）。
- **本地進階模式即時補抓**：`price_adjuster.py` + FinMind 即時。整塊 v1 沒做。PRD §M4 / §3.3。
- **tw-swing 短線訊號整合**：tw-swing 產出全是短線 → 每日短線清單打進 bundle → tw-hold 加
  「短線」唯讀分頁。合併 repo 不做，只走資料層。
- **`prices_raw_close` 每日增量**：PRD §10.3 定案改 TWSE OpenAPI 整批，尚未接（落後幾天）。
- **`_meta["split_warning"]` app UI 顯示**（現在只在 Actions log / webhook）。
- **L2/L3 上游備援**：`upstream-datapack-single-point` + `tw-swing/docs/reference/upstream_datapack_workflow.yml`。
  上游還活著，不做。

---

## 5. 紅線（一直有效）

- 🔴 **不改 tw-swing `daily.yml` / `rules.yaml` / portfolios / pipeline / 回測**（G-5 的 60 交易日時鐘）。
  可動：`fetch_fundamentals.py`、`fetch_valuation.py`、`fetch_revenue.py`、`build_u1b_bundle.py`、
  `build_per_parquet.py`、`build_revenue_history.py`、`fundamentals.yml`、`publish_bundle.yml`。
- 🔴 **PRD 凍結**：不改因子設計 / 估值公式 / 分軌。要改記 backlog、v2 再說。
- FinMind：token 600/hr、rate ≤ 450、**不 `Throttle.seed()`**、避開週六 `fundamentals.yml`（UTC 六 02:00）。
- 定存 5% 殖利率硬底線不放水到 4%。
- tw-hold 是**公開 repo**：`positions.json` / token / `.env` 絕不進版控。
- 候選池措辭：「條件成立狀態」不叫「建議進場」；「候選池」不叫「推薦清單」；不給 verdict / 買價 / 排名。
- **AI 層**：只解說、不選股、不進規則計算（`docs/AI_LAYER.md` §1）。

---

## 6. 入口點

| 要做 | 讀 |
| :-- | :-- |
| **AI 層（下一步）** | `docs/AI_LAYER.md`、記憶 `tw-hold-ai-layer-design` |
| v1 全貌 / 里程碑 | `docs/PLAN.md`（§M1–§M4 + ③ 已打勾） |
| 為什麼這樣設計 | `PRD.md`（§5 候選池、§6 價值、§7 定存、§10 失敗模式）**凍結** |
| 分割還原 | 記憶 `tw-hold-split-adjustment-gap`、`reference/corporate_actions.py`、`scripts/resolve_splits.py` |
| 月營收 / 估值每日 | 記憶 `tw-hold-revenue-history`、`tw-swing-valuation-daily` |
| 上游依賴 / L2L3 | 記憶 `upstream-datapack-single-point`、`tw-swing/docs/reference/upstream_datapack_workflow.yml` |
| 金融軌 | 記憶 `tw-hold-financial-track-design` |
| 四個回測實驗 | `docs/BACKTEST_HANDOFF.md`、記憶 `tw-hold-backtest-experiments-outcome` |
| tw-swing 現況 | `tw-swing/docs/STATUS.md` |
| 產品碼 | `build_factors.py`（`screen_all`）、`build_lists.py`、`screener/{pricing,deposit_pricing,candidate_pool,gates,screen,industry}.py`、`factors/factors.py`、`reference/{loader,corporate_actions,finmind_client}.py`、`app/{streamlit_app,bundle_data,stockcharts}.py` |
| app UI | `app/streamlit_app.py`：`_route`（`?code=`）、`_inject_css`、`_card_list` → `_card_b_html`、`_swing_page` → `_swing_b_html`、`_stock_page`。深色地基 `.streamlit/config.toml`。 |

### 個股查詢 Cloud crash（早修，教訓留著）
repo 根有 `charts/`/`reference/`/`screener/`/`factors/` 頂層套件——app 內模組不要同名。
`requirements.txt` **釘死版本**（pandas/pyarrow/streamlit/plotly），改版前本地 pytest + streamlit run + AppTest。
`_stock_page` 每圖包 `_chart` try/except。

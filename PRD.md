# tw-hold — 長波段／價值／定存決策支援工具 · 產品需求文件 (PRD)

| | |
| :--- | :--- |
| 文件版本 | **3.1-draft（未定案）**（1.0 → 2.0 → 2.1 opus 審核 → 3.0 範圍重定 → **3.1 波段軌道復活成「主動選股候選池」**，皆 2026-09-07；變更記錄見 §11） |
| 日期 | 2026-09-07 |
| 需求來源 | `me`（`taiwan-stock-analyzer-v3`）升級討論，2026-09-07 |
| 證據來源 | `me` 驗屍、tw-swing `twswing.value.factors/screen`、被動式市值型 + 高股息低波動 ETF（0050／00713／00878）的指數建構規則 |
| 撰寫視角 | **被動式指數團隊**（規則透明、定期換股、陷阱硬剔除、換手可控）＋ 高股息 ETF 分析師（殖利率永續性、填息率、總報酬） |
| 授權調整 | 使用者授權「設計衝突時採建議值並說明理由」。據此拔掉 v1.0 的「forward EPS × 市況 PE → 目標價」估值機器（理由見 §6） |
| 狀態 | 🟡 **未定案、未開工。凍結解除中**——範圍已改（砍長波段、定存重定位），且**兩條線都還沒有實證**。等 [`docs/BACKTEST_HANDOFF.md`](docs/BACKTEST_HANDOFF.md) 的實驗 B 有結果才定案 |

> # 🟡 v3.0 未定案——先讀這裡
>
> **2026-09-07 深夜，使用者重新盤點自己的資產與使用節奏之後，本 PRD 的範圍改了。**
> 舊版（v1.0–v2.2）的三清單設計有一半已經不適用，**不要照舊版動工**。
>
> ## 改了什麼
>
> | | v2.2（舊） | **v3.0（現在）** |
> | :--- | :--- | :--- |
> | 清單 | 長波段 + 價值 + 定存 | **主動選股候選池 + 價值 + 定存**（三條，但波段那條換了形態） |
> | 波段 | tw-hold 自己做，每日重算、給買入區間、不回測 | ⚠️ **v3.0 移到 tw-swing pool3 → v3.1 又搬回來**，但改成**狀態型的「候選池」**：不給 verdict、不給買價、買賣由人決定。pool3 已被回測否決，完整脈絡見 **§5.0** |
> | 定存的定位 | 「存股」＝再加一份收益 | **接手美元部位**：使用者要在一年內把債券 ETF + 特別股降低、轉 10% 資產回台股。基準不是 0050，是**他賣掉的那些東西的稅後殖利率** |
> | 上不上真錢 | 「決策支援、不上真錢」→ 據此豁免回測 | 🔴 **豁免前提消失**——實際要投入的金額比波段線還大（定存 ~200 萬分批 + 價值 ~100 萬） |
>
> ## 所以現在缺的是實證，不是規格
>
> 價值與定存的因子（F-Score、Magic Formula 式 rank、存股安全分）**在台股前 500 大
> 的實際效果從來沒有測過**。而使用者要拿定存線去替換殖利率 5–6% 的美國特別股——
> **如果過去 10 年這套規則的含息年化只有 3%、2022 年回撤 −25%，那整個轉換就不該做。**
>
> 👉 **動工前必須先做 [`docs/BACKTEST_HANDOFF.md`](docs/BACKTEST_HANDOFF.md) 的實驗 B
> （組合層歷史回溯）。** 回溯結果會決定：這兩條線值不值得做、門檻怎麼調、
> 甚至要不要換一組因子。**在那之前，本文件的因子設計一律視為假設，不是定案。**
>
> ---
>
> **架構（v2.2，三個 repo）**：資料層獨立成 **`tw-data`（公開）**——它抓 FinMind／
> 上游資料包，整併成 data bundle，發佈到**自己的公開 Release**。
> **`tw-swing`（私有）** 與 **`tw-hold`（私有）** 都只是**消費者**，
> 用**匿名 HTTP** 拉 bundle（公開 Release 不需要 token）。
> tw-hold 做長波段／價值／定存的 domain 邏輯與 UI，佈署上網；
> 執行期對 `twswing` package **零依賴**。
>
> `me` 原始碼：`d:\g\claude\books\claude\me\taiwan-stock-analyzer-v3\`。

---

## 1. 產品定位

### 1.1 一句話

上網跑的台股決策支援工具，三個產出：

| 產出 | 頻率 | 性質 |
| :--- | :--- | :--- |
| **主動選股候選池**（§5） | 週 | **資訊**——候選 + 支持/反對證據 + 風險量化。**不給 verdict、不給買價**，買賣由人決定 |
| **價值清單**（§6） | 月看、季換 | 推薦清單（買入區間 + 不推薦原因） |
| **定存清單**（§7） | 季 | 同上。用途是**接手美元收益部位**（§7.0） |
| 個股查詢（§4） | 隨時 | 不打分，數據 + 圖表 |

> v3.1：波段那條從 v3.0 的「移出」改回「留下但換形態」。**它跟另外兩條的性質不同**
> ——那兩條給建議（有回測門檻要過），這條只給資訊（所以不宣稱報酬）。

### 1.2 唯一驗收標準

> **打開來看到的是決策，不是一個黑箱總分。**

`me`（`taiwan-stock-analyzer-v3`）投入 1,420 行評分、858 行交易建議，結論是
「什麼都做，做完卻沒鳥用」。三個死因，本 PRD 的設計都在迴避：

| `me` 死因 | 機制 | tw-hold 對策 |
| :--- | :--- | :--- |
| **一個總分 → 買第一名** | 25 個維度加權平均成一個分數，「某支看起來不錯」就變成「把總分第一名買下去」 | **先硬門檻剔除、再透明因子 rank**；價值/配息陷阱不被其他分項高分蓋過 |
| **只有進場、沒有出場** | 給買價，不給「什麼時候這個理由不成立了」 | 清單＝當期成分股；**離開成分＝出場訊號**，每期揭露「移除 + 原因」；長波段另有進場前寫死的出場規則檢查表 |
| **估值靠外推疊外推** | forward EPS 外推 × 市況調整 PE × 安全邊際，誤差相乘卻只顯示一個數字 | 估值錨到**個股自身歷史**與**當下買點鎖進的殖利率**，不外推、不做市場擇時 |

### 1.3 三個 repo 的分工（v2.2）

| | **tw-data**（公開） | **tw-swing**（私有） | **tw-hold**（私有） |
| :--- | :--- | :--- | :--- |
| 角色 | **唯一的抓取者** | 消費者 | 消費者 |
| 用途 | 抓 FinMind／上游資料包 → 整併 → 發佈公開 Release | **短線（日，pool1/2）**，全市場掃描、回測、資金池、真錢門檻。⚠️ pool3 週波段**已回測否決**（§5.0） | **主動選股候選池（週）+ 價值（月）+ 定存（季）**，決策支援、不進資金池 |
| 交易系統 | — | 是（gate + 樣本外 + 熔斷） | **否**——不進資金池、不走 gate |
| 產出 | bundle（公開） | 每日候選清單、模擬單（**私有**） | 三清單 + 個股頁（**私有**） |
| 對別人 | 不知道下游是誰，只負責把資料弄對 | 匿名拉 bundle | 匿名拉 bundle、不碰 tw-swing 的 rules/pools/pipeline |

> **為什麼資料層要獨立成 repo**（v2.2 改判，理由見 §3.1）：v2.1 讓 tw-swing
> 「順便當上游」，結果是兩件事綁在一起——**tw-swing 的關鍵路徑（G-5 時鐘）與
> tw-hold 的資料需求擠在同一個 repo、同一份 Actions 額度、同一條紅線上。**
> 拆出 tw-data 之後，抓取的失敗不會碰到任何人的關鍵路徑，且公開 repo 的
> Actions **免費無上限**、公開 Release **匿名可下載** → 兩顆 PAT 一起消失。

### 1.4 這不是 `me` 的哪裡

`me` 把分數放在決策位置——那個批評對**波段**成立。但 tw-hold 的價值/定存清單不是
逐檔深度估值，而是**規則化因子指數**（想成台版 Magic Formula / 00713 高股息低波動）：
用透明因子 rank 選成分、季換股、陷阱硬剔除。**同一個「分數」，`me` 拿去買第一名是
死因，這裡拿去排 rank 並每期揭露換股是正解。**

⚠️ **但這個論證有個前提：rank 選出來的組合真的有超額報酬。** 那件事在台股前 500 大
沒有被驗證過（v3.0 發現）。`me` 的死因是「分數放在決策位置」，而**一個沒被驗證過的
分數放在決策位置，跟 `me` 是同一件事**——只是包裝成指數化而已。實驗 B 就是在檢查
這一點。

---

## 2. 兩清單的評估法

### 2A. 共用紀律（兩清單都套）

1. **可投資宇宙**：前 300–500 大市值 ∩ 日均量 > 門檻 ∩ 非處置／全額交割。
   - ⚠️ **市值 ≠ 成交值**。上游 `fetch_fundamentals.py` 的前 500 是用**成交值**排的，
     它自己的註解就警告「最安靜的大型定存股（某些金控/傳產）成交值可能排不進 500」
     ——那正是定存清單最想要的股票。因此 `universe.parquet` 的宇宙 =
     **市值前 500 ∪ 成交值前 500**（U3 負責，見 §3.1），tw-hold 再套日均量門檻。
2. **陷阱＝硬門檻剔除**，不被其他分項高分蓋過（先剔除、再排序）。
3. **因子透明、合理預設、不對績效調參**（與 tw-swing `regime` 門檻同一種紀律）。
4. **離開清單＝出場訊號**：每次重算輸出「本期新進 / 移除 + 移除原因」。
5. **換手率揭露**：每期換掉幾檔要顯示。

### 2B. 組合建構機制（分軌）

| 清單 | 想成哪種基金 | 建構機制 | 頻率 | 價格 / verdict |
| :--- | :--- | :--- | :--- | :--- |
| **主動選股候選池** | 主動成長股經理人（CANSLIM / SEPA） | **無組合建構**——狀態型條件篩出候選，不排名次、不配權重（§5） | **週** | 🔴 **無 verdict**。只給風控推導的可買上限與失效條件（§5.3） |
| **價值** | 價值因子指數（台版 Magic Formula） | 季換股（3/6/9/12，財報公告後）、rank(品質)+rank(便宜)、單一產業 ≤ 40% | **月看、季換** | 你買進的殖利率（主）+ 自身歷史 PE 分位帶（§6） |
| **定存** | 高股息低波動 ETF（00713 式） | 季換股、殖利率+品質+低波動+填息率複合分、產業上限 | **季** | 殖利率法買價（§7） |

⚠️ **第一條跟後兩條的性質不同**：後兩條是**組合**（有成分、有換股、有權重、要回測驗證）；
第一條是**候選池**（沒有組合概念，不宣稱報酬，買賣由人決定）。不要用同一套 UI 邏輯做。

- 清單長度 **10–20**，限市值前 500 大。
  - ⚠️ 但**實際資金決定實際檔數**：價值 ~100 萬 → 4–6 檔；定存 ~200 萬（一年分批）
    → 8–12 檔。台股一張 1000 股，中大型股一張 5–50 萬。清單可以長，**建議建倉的
    檔數要對得起資金**，否則產業上限 40% 之類的規則在 3 檔的時候根本不會生效。
- 每檔輸出：排序 + 量化理由 + 買入建議價 或「不推薦」（明確原因）。

---

## 3. 資料層：bundle 發佈與拉取

### 3.1 tw-data 發佈（公開 repo、公開 Release、匿名可下載）

打包核心宇宙（前 500 大）資料，發佈到 **`tw-data` 自己的公開 Release**
（移動 tag `data-latest`）。**下游匿名 HTTP 下載，不需要任何 token。**

> **v2.2 改判：從「tw-swing 私有 Release + PAT」改成「tw-data 公開 Release」。**
> v2.0→v2.1 曾把「公開」否決掉，理由是「使用者只有一個人，公開沒有任何好處」。
> **那個前提在 v2.2 不成立了**——公開帶來三個具體好處：
>
> | | 好處 |
> | :- | :--- |
> | **額度** | 公開 repo 的 Actions **免費且不計帳號額度**。抓取是最吃分鐘數的部分（估 ~490 分/月），整段移出私有額度（§10.1） |
> | **兩顆 PAT 消失** | 公開 Release **匿名可下載** → 下游不用 read PAT；tw-data 發自己的 Release → 用內建 `GITHUB_TOKEN`，不用 write PAT。**守門員 G6（PAT 過期）整條刪掉** |
> | **結構** | 資料層有自己的家，不再是「tw-swing 順便當上游」。抓取失敗不再碰到 tw-swing 的關鍵路徑 |
>
> **仍然成立的顧慮**：FinMind 專案本體是 Apache 2.0 但明寫「教育、非商業用途」，
> 公開一份整併資料集有「免費鏡像」的觀感。判斷：原始資料是 TWSE/TPEx 公開資訊、
> 我們發佈的是衍生計算，法律風險低；**現在有了具體好處，成本效益翻轉**。
> repo 體積的顧慮早就被「Release 資產不進 git 歷史」解掉。
>
> ⚠️ **「tw-data 公開」不等於「tw-swing / tw-hold 公開」**——那兩個維持私有，
> 清單、模擬單、持倉都不外流。公開的只有**市場資料本身**。

**bundle 分兩條管線發佈**（管線代號沿用 U1a / U1b，只是家從 tw-swing 搬到 tw-data）：

| bundle 檔 | 來源 | 頻率 | 管線 |
| :--- | :--- | :--- | :--- |
| `fundamentals/{income,balance,cashflow,dividend}.parquet` | `fetch_fundamentals.py`（**從 tw-swing 搬進 tw-data**） | 週 | **U1a** |
| `per.parquet`（逐日 PER/PBR/殖利率，**整併成一張表**） | `fetch_finmind.py` 的 `per`（現為逐檔，同上搬家） | 週 | **U1a** |
| `universe.parquet`（當期宇宙 + 市值 + 產業別） | **新寫**（`twswing.data.universe` 沒有市值/產業，見下） | 週 | **U1a** |
| `prices_adj.parquet`（前 500 大、已還原日線、近 5–8 年） | tw-data 自己抓上游資料包 | 日 | **U1b** |
| `prices_raw_close.parquet`（未還原收盤，**填息率要用**） | TWSE OpenAPI 每日整批（歷史一次性回補，見 §10.3） | 日 | **U1b** |
| `revenue.parquet`（月營收 + `available_date`） | 月營收快照邏輯（移植自 `twswing.data.fundamentals`） | 日 | **U1b** |
| `index_0050.parquet`（大盤代理收盤序列；**§5.2.2 的 RS 排名要用**） | 同 `prices_adj` | 日 | **U1b** |
| `chips.parquet`（法人買賣超，**§5.2.1 的「法人 20 日淨買超」要用**）⚠️ **v3.1 新增** | TWSE 整批檔（tw-swing 已在抓） | 日 | **U1b** |
| `_meta.json`（版本、產生時間、涵蓋範圍、每檔行數、**本次含哪些檔**） | publish step | 每次 | 兩者 |

### 3.1.1 tw-data 的兩條管線，與那條不可碰的紅線

| 管線 | workflow | 內容 | 頻率 |
| :--- | :--- | :--- | :--- |
| **U1a** | `tw-data/.github/workflows/weekly.yml` | 財報四表 / PER 整併 / universe（含市值、產業別） | 週 |
| **U1b** | `tw-data/.github/workflows/daily.yml` | 前 500 大日線（還原）/ 未還原收盤 / 月營收 / 0050 | 日 |

- 兩條都住在 **tw-data**，Actions **免費無上限**，跟 tw-swing 的額度與紅線**完全脫鉤**。
- tw-hold 端：`fetch_bundle.py` 要能在**只有 U1a 資產**時正常運作（缺 U1b 檔就標記
  「日線類特徵不可用」而不是拋錯）——這樣 U1a 一落地就能開工，不必等 U1b。

🔴 **唯一的紅線：不修改 tw-swing 的 `daily.yml`。**

- tw-swing 的 `daily.yml` 在 **G-5 的 60 交易日時鐘**上，鎖 11 月下旬，
  **斷一次重算 60 天 → 跳明年 2 月**。它是唯一擋在「上真錢」前的東西。
- 所以 **tw-data 自己抓，不從 tw-swing 拿**。重複下載上游資料包（220MB）不花錢、
  不吃 FinMind 額度，換來的是兩條管線完全獨立。
- ⚠️ **tw-swing 改吃 tw-data 的 bundle 是「第二期」的事，排在 G-5 過關之後**
  （見 §11 v2.2 的分期）。第一期 tw-swing **一個字元都不動**。

### 3.1.2 資料怎麼從舊專案接過來（tw-data 的起始資料）

tw-data 是新 repo，但**不是從零抓**——tw-swing 已經有的直接搬：

| 要的東西 | 從哪來 | 可不可回補 |
| :--- | :--- | :--- |
| 財報四表（514 檔、2015Q1–2026Q2） | tw-swing `data/fundamentals/*.parquet`（**已版控**，直接複製） | 可（但沒必要重抓） |
| 日線（還原） | 上游資料包（tw-data 自己抓一次即可重建全歷史） | 可 |
| 未還原收盤（5 年） | FinMind `TaiwanStockPrice` 本地一次性回補（§10.3） | 可 |
| **處置股快照** | tw-swing `data/watchlist/`（**已版控**，直接複製） | 🔴 **不可**——端點只給當前清單 |
| **月營收快照** | tw-swing `data/revenue/`（**已版控**，直接複製） | 🔴 **不可**——同上 |
| PER 逐日 | FinMind `TaiwanStockPER`（本地已有，或重抓 500 req） | 可 |

- ⚠️ 那兩份**不可回補**的快照剛好都在版控裡——**搬 repo 之前先確認複製到位**，
  它們是 2026-08-27 起才開始累積的，弄丟就永遠是空的。
- 搬完之後 **tw-swing 那兩份繼續留著**（`daily.yml` 還在寫它們，第一期不動）。

- 🔴 **市值資料源是 M0 的硬前置，現在還沒解決**（opus 審核 2026-09-07 發現）：
  - `twswing.data.universe` 目前只有 `stock_list()` / `equity_tickers()` /
    `restrict_to_equities()`——**沒有市值、沒有產業別**。U3 是從零寫，不是 re-export。
  - 市值 = `收盤價 × capital_stock / 10`，但 tw-swing STATUS 明寫
    「`cash`／`capital_stock` 兩欄**補抓當掉**」。在 tw-swing 那邊這是 nice-to-have
    （暫用 PBR × 股東權益），在 tw-hold 這是**宇宙定義的基礎**。
  - → **M0a 第一件事：修好 `capital_stock` 補抓**。沒修好就用 PBR × 股東權益起步，
    但要在 `_meta.json` 標記市值口徑，且 UI 顯示「市值為估算值」。
  - ⚠️ 這條沒解，定存清單會**系統性漏掉核心標的且不會報錯**（tw-swing 提醒 #15
    「不會拋錯的錯」）。
- ⚠️ **產業別**：FinMind `TaiwanStockInfo.industry_category`（§7.4 產業集中度要用）。
- ⚠️ **PER 整併成一張表**：估值分位帶（§6）要用。

### 3.2 tw-hold 拉取與重算

- `reference/`：接收 tw-swing 的共用碼，不再 import：
  `loader.py`（parquet → 季度面板 + 45 天公告日遞延，72 行）——⚠️ **是搬移不是複製，
  tw-swing 端刪除**（理由見 `tw-swing/PRD.md` §7.1.1：搬完之後那邊零消費者、零測試）、
  `regime.py`（顯示旗標）、
  `finmind_client.py`（僅個股即時查用）、`indicators/{core,ma_rules,pivots,trendlines}.py`
  （長波段 screener）、`price_adjuster.py`（移植自 `me`，僅 500 大以外用）。
- `fetch_bundle.py`：**匿名 HTTP** 從 tw-data 的**公開** Release（tag `data-latest`）
  下載 → `data/upstream/`（gitignore）+ 記 `_meta.json`。**不需要 token。**
  - ⚠️ **開場就驗 schema**：`_meta.json` 帶 `schema_version` 與每檔的欄位清單，
    對不上就**大聲失敗**（見 §10 守門員 G1）。這是兩個 repo 之間唯一的正式介面。
- **tw-hold 排程 Action（每日跑）**：`fetch_bundle.py` → `build_factors.py`
  → 產出 `data/derived/factors.parquet` + `lists/{longswing,value,deposit}.json`
  （含本期成分、rank 明細、買價、verdict、**新進/移除清單**）→ commit 回 tw-hold repo。
  - ⚠️ **「新進/移除」需要上一期成分當輸入**：`build_factors.py` 必須明確讀
    `data/derived/lists/*.json` 的**前一版**再比對，不可以靠 git diff（那不是資料流、
    也重跑不出來）。上一期成分寫進 `lists/*.json` 的 `previous` 欄位，自我包含。
  - 財報週更、價量日更 → 每日重算讓長波段清單技術面不過期；運算量趨近 0（讀 parquet + pandas）。
- **tw-hold app 的資料有兩條路，不是一條**（opus 審核 2026-09-07 補；原本只寫了第一條，
  結果個股頁在雲端會沒有資料可讀）：

| 頁面 | 讀什麼 | 雲端可用 |
| :--- | :--- | :--- |
| **三清單**（主要用途） | 自己 repo 的 `data/derived/`（每日 Action commit 進來） | ✅ 直接可用，運算量趨近 0 |
| **個股查詢頁**（M4） | **bundle 本身**——但 `data/upstream/` 是 gitignore 的，**不在佈署出去的 repo 裡** | ⚠️ 要 app 執行期自己拉一次（見下） |

- **個股頁的雲端資料路徑**：app 執行期**匿名**拉 bundle（公開 Release，不用 token），
  `st.cache_resource` 快取住，一個容器只拉一次。
  - 冷啟多 10–20 秒（bundle 壓縮後估 30–50MB）。
  - 🔴 **必須按需讀取**：`pyarrow` 只讀該檔股票的 row + 該張圖要的欄位
    （`filters=[("ticker","==",x)]`, `columns=[...]`），
    **不可以 `pd.read_parquet()` 整張載進來**——Streamlit Cloud 只有 1GB RAM。
  - 前 500 大以外：雲端一律回「不在核心宇宙，請用本地進階模式」（§3.3）。
- **兩條路都不依賴 tw-swing**，只依賴 tw-data 的公開 Release 還在。

### 3.3 個股即時補抓（僅本地）

- 用 `reference/finmind_client.py`。快取 `data/cache/<ticker>/`（gitignore）。
- TTL：財報 30 天、PER 7 天、股價 1 天、月營收 7 天。每 ticker 一個 `_meta.json`。
- 雲端偵測環境變數 → **停用此功能**，只回 bundle 內前 500 大。
- FinMind 排程與 **tw-data** 錯開、守 500/hr。**不開新帳號**（GitHub、FinMind 都是；
  理由見 §9 已定 10）。真卡到 → tw-data 改抓 TWSE OpenAPI。
- ⚠️ v2.2 之後 **FinMind 的所有排程抓取都集中在 tw-data**，「單一管線」這條紀律
  變得更好守：本地個股補抓是唯一的例外，而它是互動觸發、不會跟排程撞。

---

## 4. 個股查詢 + 圖表

### 4.1 個股查詢（不打分）

- 輸入代號 → **前 500 大**：讀 bundle（雲端走 §3.2 的執行期拉取 + 按需讀取路徑）；
  **不在**（僅本地）：即時打 FinMind 補那一支 → `data/cache/<ticker>/`。
  雲端版只服務前 500 大——**八類圖表在雲端都畫得出來**，bundle 的欄位是齊的。
- **不給評分、不給買賣 verdict**。給：原始數據表 + 圖表（§4.2）+ Piotroski F-Score
  9 分項當診斷清單（打勾/打叉，**不加總**）。
- 精神：攤開數據讓人 / AI 判斷，不代替判斷。

### 4.2 圖表清單（參考 `me/ui/waterfall_charts.py`，升級為 plotly 互動圖）

| 類別 | 圖 |
| :--- | :--- |
| 價格 | K 線 + 均線（周/季/年線）+ 量；相對大盤強弱（vs 0050） |
| 成長 | 月營收 YoY / MoM 柱狀；季度 EPS；年度 EPS |
| 獲利品質 | 三率趨勢；ROE / ROA |
| 財務結構 | 負債比 / 流動比；現金與約當現金 |
| 現金流 | OCF / FCF / 資本支出 逐季 |
| 股利 | 逐年現金 + 股票股利；殖利率 + 股價；**填息天數 / 填息率** |
| 估值 | **本益比河流圖**（台股經典）；PBR 河流圖；normalized PE vs trailing PE + 分位帶 |
| 診斷 | Piotroski F-Score 9 分項打勾表（不加總） |

技術：**plotly**。`me` 的圖搬成 plotly、不重新設計；只有本益比河流圖值得做好。

### 4.3 AI 敘事層（選配，後期）

「複製給 AI」：數據 + 因子整理成文字 → 貼給 GPT/Gemini/DeepSeek。只傳整理過的數據。

---

## 5. 主動選股候選池（v3.1 復活——但形態完全不同）

> **v3.0 曾把這一軌砍掉**（移到 tw-swing 的 pool3）。**v3.1 復活**，因為 pool3 的
> 回測把當初砍它的理由推翻了一半——而復活的**不是原本那個東西**。
> 差別見 §5.0。

### 5.0 三次改判的完整脈絡（不看這節會重蹈覆轍）

| 版本 | 這一軌是什麼 | 結局 |
| :--- | :--- | :--- |
| v2.2 | tw-hold 自己做長波段清單：季線 + RS + 月營收，每日重算，**給買入區間、不回測** | ❌ 砍掉——要投 200 萬卻沒停損、沒部位控制、沒驗證 |
| v3.0 | 移到 tw-swing `pool3`：Y4/Y1 + 長出場 + 週批次，**走完整回測門檻** | ❌ **回測否決**（見下） |
| **v3.1** | **回 tw-hold，改成「主動選股候選池」**：狀態型條件、不給 verdict、買賣由人決定 | 本節 |

**pool3 為什麼被回測否決**（`tw-swing/docs/reports/backtest_20260907-84247a0-pool3_exit.md`
＋ `Y4_swing_memo_20260907.md`）：

- **A-1 換長出場**：沒有任何一條變體同時通過四門檻與樣本外。過四門檻的
  Y1-plow10/plow20 樣本外翻負（−0.14% / −0.43%）；樣本外撐住的 Y4-plow*
  全卡 ②a MAR（0.54–0.63 < 0.71）。通過的 Y1/Y4 **平均只持有 5.0 個交易日**
  ——那是短線，不是波段。
- **A-2 延後進場**：Y4 +1 日開盤每筆超額從 +1.48% 掉到 **+0.32%**（掉七成）；
  **+2 日樣本外翻負 −0.36%**。而週批次的實際延遲是 +1～+5 天的分布。
- 連 +1 日那格也在雜訊裡：樣本外 +0.58%，n=359，標準誤約 0.44% → **約 1.3 個
  標準誤，統計上跟 0 分不開**。
- **絕對報酬還輸大盤**：Y4 年化 +16.88% vs 0050 +24.12%（Y4 只贏回撤 −7.2%
  vs −33.8%）。而使用者 75% 資產已在特別股／債券——低波動他已經有了。

> ## 這個實驗真正的結論
> **在這個地基上（日線 + 月營收 + PE），「訊號有超額報酬」和「一週看一次」是衝突的。**
> Y4 的超額集中在突破後兩週的加速段——那是**事件式技術訊號的本質**，
> 不是 Y4 的缺陷。這也解釋了候選庫為什麼會乾：不是挖得不夠，
> 是「低頻 + 高超額」在這批免費資料上可能不存在。

### 5.0.1 所以復活版換掉了什麼

**放棄「訊號有超額報酬」這個宣稱，換取「一週看一次」。**

| | pool3（否決） | **v3.1 候選池** |
| :--- | :--- | :--- |
| 條件型態 | **事件**（軌道突破，當天成立、隔天過期） | 🔴 **狀態**（趨勢排列 + 成長，可連續數週成立） |
| 延後進場 | 掉七成 | **無此問題**——不是事件，沒有「錯過了就沒了」 |
| 產出 | 推薦清單 + 買入區間 + verdict | **候選池 + 支持/反對證據**，買賣由人決定 |
| 要不要回測 | 要（走四門檻） | **不宣稱報酬，所以不需要**——但也**不准宣稱報酬** |
| 資金池／停損 | 系統管 | **人管**（停損掛券商） |

🔴 **這個交換是使用者明確做的**（2026-09-07：「要不要買，我自己決定」）。
代價是：**這條線沒有任何績效保證**。它只回答「現在哪些股票處在強勢的位置」，
不回答「買了會不會賺」。

### 5.1 四條守則（沒守住就變回 `me`）

> `me` 的死因不是資訊多，是**它以為自己在給資訊，實際上給了一個排序好的總分**。
> 差別不在你怎麼稱呼它，在**它有沒有給你一個可以照做的動作**。

1. **不給 verdict**（推薦／不推薦）、**不給目標價／合理價**——沒有估值錨就別編。
2. **不合成單一總分**。多維度分開顯示，不加權平均，**不排「第一名」**。
3. **每檔強制並列「支持」與「反對」兩欄**。強迫呈現反面，是防止「看起來不錯就買」
   最有效的一招。反對欄空白的標的要標記為「檢查不足」，不是「完美」。
4. **必須給「理由失效條件」**。這不是建議，是資訊的一部分；沒有它就會重蹈
   `me` 的第二個死因（只有進場、沒有出場）。

### 5.2 選股邏輯：CANSLIM（歐尼爾）+ 趨勢模板（Minervini）

🔴 **全部寫成「狀態」，不准出現「今天突破」這類事件條件**——那是 pool3 的死因。

**5.2.1 基本面（CANSLIM 的 C / A / N / S / I）**

| 條件 | 資料來源 | 有沒有 |
| :--- | :--- | :--- |
| 最近一季 EPS YoY > 25% | 財報（前 500） | ✅ |
| 近三年年度 EPS 成長 | 財報 | ✅ |
| **月營收 YoY 為正且加速**（`revenue_accel`，Y4 唯一該留下的東西） | 月營收全市場快照 | ✅ |
| ROE > 15%、毛利率未連兩季惡化 | 財報 | ✅ |
| Piotroski F-Score ≥ 6 | 已有 `factors.py` | ✅ |
| 法人近 20 日淨買超 | 籌碼（bundle 要加，見 §3.1） | ⚠️ **新增需求** |

**5.2.2 趨勢模板（Minervini 八條，全部只要日線）**

```
① 股價 > 150MA 且 > 200MA
② 150MA > 200MA
③ 200MA 上升至少一個月
④ 50MA > 150MA > 200MA
⑤ 股價 > 50MA
⑥ 股價高於 52 週低點 30% 以上
⑦ 股價在 52 週高點的 25% 以內
⑧ 相對強度 RS 排名 > 70（vs 0050）
```

- 顯示 **8 條中過幾條**（例如「趨勢模板 7/8，缺 ⑦」），**不加總成分數**（守則 2）。
- 這八條加上基本面，一檔股票可能**連續數週都成立**——這正是週頻合理的原因。

**5.2.3 理由失效條件（守則 4）**

| 失效條件 | 對應「理由不成立了」 | 檢查頻率 |
| :--- | :--- | :--- |
| 跌破 50MA（收盤） | 強勢結束 | **掛券商停損單**，不靠開網頁 |
| 趨勢模板掉到 5/8 以下 | 趨勢排列瓦解 | 週 |
| 月營收 YoY 由正轉負連 2 個月 | 成長沒了 | 月 |
| 最新季 EPS YoY 轉負 | 同上 | 季 |

### 5.3 價格：給「風險多大」，不給「值多少」

**價值/定存的區間上緣由估值決定；波段的上緣由風控決定。** 成長股沒有可靠的
估值錨，而且免費層沒有預估獲利（那台機器 v2.0 已經刪掉，§6.2）——
**給估值型區間等於宣稱一個我們沒有的能力。**

但「風險多大」是算得出來的，那不是預測：

```
停損參考位 = max( 50MA , 近 20 週前低 , 現價 − 2×ATR14 )
             ↑ 結構性支撐；跌破代表「強勢」這個前提沒了

風險%      = (現價 − 停損參考位) ÷ 現價

可買上限   = 停損參考位 ÷ (1 − 10%)
             ↑ 推導：部位 = min(20%, 單筆風險% ÷ 停損距離)。
               停損距離 > 10% 時部位縮到 5% 以下，不值得做。
               「還能不能買」＝「以這個價買，部位還撐不撐得起來」
```

另給**位置揭露**（純資訊）：距 50MA、距 52 週高、距 200MA 各幾 %。

**範例輸出**：

```
2317 鴻海　現價 218
停損參考位 203（50MA）｜風險 6.9%｜可買上限 226
位置：距 52 週高 −8%、距 200MA +19%
支持：月營收 YoY +32% 且加速、法人 20 日淨買超、趨勢模板 8/8
反對：毛利率連兩季下滑、季 EPS YoY 僅 +11%（低於 CANSLIM 的 25%）
失效：跌破 50MA｜月營收 YoY 轉負連 2 個月
```

🔴 **這句話要印在 UI 上，不是只寫在 PRD 裡**（半年後你不會記得）：

> **這個區間沒有回測支撐。它是風控算術，不是驗證過的買點——
> 只回答「以這個價買會承擔多少風險」，不回答「這個價買會不會賺」。**

> 三條線的價格邏輯因此是一致的：**都是「不追高 + 風險有界」**，
> 只是短線的上限錨在訊號價、波段錨在支撐位、價值/定存錨在估值。

### 5.4 可行性：先算「每週剩幾檔」（動工前置）

CANSLIM 門檻很嚴（EPS YoY > 25% + 趨勢模板）。**在台股前 500 大裡，
每週可能只剩 0–5 檔，空頭時段可能一檔都沒有。**

- 使用者已確認 **0–5 檔可以接受**（2026-09-07）。歐尼爾自己也說空頭時清單就該是空的。
- 但要先普查一次，確認**不會常年空白、也不會常年 50 檔**（那代表門檻沒作用）。
- 見 `docs/BACKTEST_HANDOFF.md` **實驗 D**。

### 5.5 使用節奏

**週末開一次網頁**（看候選 + 看持倉的失效條件），**停損掛在券商**。
不需要每天開——狀態型條件週三看跟週五看是同一批。


### 5.9 ~~出場規則（v2.2 原版）~~ ❌ 已作廢，保留供對照

> v3.1 的失效條件見 §5.2.3。下表是 v2.2 那個「每日重算 + 給買入區間」版本的殘留。

清單每次重算時，對「已在清單內」的標的標示 **續抱 / 出場**，任一成立即出場：

| 出場條件 | 對應「訊號消失」 |
| :--- | :--- |
| 跌破季線且季線轉下彎 | 趨勢沒了 |
| 相對強度（vs 0050）近 4 週轉負 且 跌破前波低 | 動能沒了 |
| 月營收 YoY 由正轉負連 2 個月 / 最新季 EPS YoY 轉負 | 基本面順風沒了 |
| 進場滿 12 週未創新高 或 未達 +8% | 時間停損（機會成本） |

- **輸出**：清單 + 進場理由 + **條件成立區間**（不是「建議進場區間」，見上）+
  **每檔的出場條件檢查表**。
- 🔴 **這張表需要「持倉狀態」，而架構是無狀態的**（opus 審核 2026-09-07 發現）：
  「進場滿 12 週未創新高 / 未達 +8%」要**你自己的進場日與進場價**，而
  「**在清單內**」≠「**你持有**」——兩者的 12 週起算點完全不同。
  → 加一個最小持倉登錄 `data/positions.json`（手動填 `代號 / 進場日 / 進場價`，
  不需要 UI、不需要下單記錄）。沒有它，四條出場規則有一條算不出來，另外三條也只能
  回答「這檔還在不在清單上」，回答不了「你該不該賣」。
- **風報比**取代目標價：到出場觸發價（季線）的距離 = 風險，到前波高的距離 = 報酬。
- **不進資金池**（不上真錢，故豁免 tw-swing 的 gate / 樣本外 / 熔斷）。
- **v1 不回測，但 v1.5 要跑一次**（opus 審核 2026-09-07）：§5.1 進場 + §5.3 出場已經
  **整套寫死**了——出場寫死正是可回測的前提，這份規格已經意外地把驗證門檻降到很低。
  v1.5 拿 tw-swing 回測引擎跑**一次**，只看「期望值是不是負的」，**不調參**
  （調參會違反凍結條件 4）。~1 session。
- ⚠️ **這是 tw-swing 第二條紅線的界線**：「訊號不得提前當建議」。長波段可以不回測，
  但在跑過那一次之前，輸出的措辭是「**條件成立區間**」不是「建議進場區間」——
  差別很小，但它決定了這是決策支援還是未驗證的交易建議。

---

## 6. 價值清單（價值因子指數）

### 6.1 建構機制

- 可投資宇宙內，**季換股**（3/6/9/12 月中、財報公告後；對齊 Q1 5/15、Q2 8/14、Q3 11/14、Q4 3/31，實作時定）。
- `screen_value()`（tw-swing 現有邏輯，M0 搬進 tw-hold）留用：
  - **品質 gate**：Piotroski F-Score ≥ 6，`< 6` 直接剔除。
  - **價值陷阱剔除**：營收連 3 季衰退 / 毛利率 5 年趨勢向下 / 近 3 年 FCF 有 2 年以上為負。
  - **rank**：`quality = rank(f_score, roe)`、`cheap = rank(norm_ey, fcf_yield, ev_ebit⁻, net_cash)`，
    `value_score = (quality + cheap) / 2`。
- 取前 10–20、**等權** + 單一產業 ≤ 40%。
  - ⚠️ v2.0 寫的「單檔 ≤ 30%」是**空條件**：10–20 檔等權 = 單檔本來就是 5–10%，
    那個上限永遠不會生效。已刪除，不要再實作它。

### 6.2 買價怎麼給（v1.0 拔掉「目標價引擎」後，兩個都給——使用者裁決 2026-09-07）

**為什麼拔掉 v1.0 版**：`目標價 = forward_EPS × 市況調整PE` 疊了三層脆弱——
(a) `forward_EPS = TTM × (1+g)` 在雜訊很大的季資料上外推；(b) 市況 target_PE 15/20/22
是**市場擇時**、且是整條公式擺動最大的因子；(c) 誤差相乘，信賴區間巨大卻只顯示一個數字。

替代：**主表顯示「你買進的殖利率」，展開明細再給「估值上緣」**。

| | 內容 | 位置 |
| :--- | :--- | :--- |
| **① 你買進的殖利率** | 盈餘殖利率（normalized）、FCF 殖利率、EV/EBIT——**不預測價格**，只講這個買點鎖進什麼報酬率。價值 ETF 買家實際拿到的就是這個 | **主表主欄** |
| **② 估值上緣**（參考，非預測） | `估值上緣 = TTM_EPS（當期，不外推）× P70(自身近 5 年 trailing PE)`——當期盈餘 × 該股自己過去的估值高位。給一個「空間 %」量級感，明講是天花板不是目標 | **展開明細** |
| **便宜門檻**（「推薦」的 gate） | `便宜門檻 = normalized_EPS × P30(自身近 5 年 PE)`——CAPE 盈餘 × 低分位、保守 | 展開明細 |

- ⚠️ **盈餘基礎不對稱**：估值上緣用 `TTM_EPS`（避免懲罰成長股），便宜門檻用
  `normalized_EPS`（近 5 年年度 EPS 均值，攤平景氣循環、偏保守）。**這個不對稱站得住**
  ——拿掉它會反過來懲罰真成長股。
- ⚠️ **但真正的風險在景氣循環股的獲利高峰**（opus 審核 2026-09-07）：TTM_EPS 在峰值
  → 估值上緣被吹高 → 「空間 %」顯示得很誘人。**加一個顯示旗標**：
  `TTM_EPS / normalized_EPS > 1.5` 時，明細標
  「⚠ 盈餘可能在循環高位，估值上緣偏樂觀」。旗標只顯示，不進公式、不進 verdict。

### 6.3 verdict（明確、寫死）

| 條件（由上往下，先命中先算） | verdict |
| :--- | :--- |
| F-Score < 6 / 營收連 3 季衰退 / 毛利 5 年下滑 / FCF 3 年內 2 年負 | **不推薦（品質）** |
| 日均量 < 門檻 | **不推薦（流動性）** |
| 現價 ≥ 便宜門檻 | **觀望（無安全邊際）** |
| 現價 < 便宜門檻 | **推薦** ＋ 標估值帶、買入區間 |

> 🔴 **v2.0 的「現價 > 估值上緣 → 不推薦（估值偏貴）」已刪除**（opus 審核 2026-09-07）。
> 理由：§6.2 明寫估值上緣是「參考，非預測」「是天花板不是目標」，但 v2.0 又拿它當
> verdict gate——**一個進了 verdict 的數字就不是參考了**。那正是 `me` 的死因在小規模
> 重演：一個你聲明「不要拿它做決定」的數字，實際上在做決定。
> **verdict 只由便宜門檻一個數驅動**；估值上緣只用來算展開明細裡的「空間 %」。

```
下界 = max(季線, 近 20 週前低)
上界 = min(便宜門檻, 現價)
買入區間 = [下界, 上界]   if 下界 <= 上界
         = 不顯示區間，改標「支撐位（{下界}）已在買點之上 → 等回檔或等支撐下移」
```

⚠️ **區間可能倒置**（opus 審核 2026-09-07）：verdict 為「推薦」時上界 = 現價，
而下界 `max(季線, 前低)` 可能高於現價（剛跌破季線的便宜股）。**不可以硬吐一個
負寬度的區間**，要走上面那條分支。§7.3 同一個公式同一個處理。

### 6.4 展示

每檔附：`value_score` 與品質/便宜 rank 明細、normalized_EPS 與 TTM_EPS、PE 分位帶
（P30/P70）、現價位置、你買進的殖利率、估值上緣與空間 %、買入區間，及「不推薦」
時的確切原因。

### 6.5 市況（`regime.classify()`）

**只當顯示旗標**（"目前 chop"），不進任何選股或估值公式。被動框架不做市場擇時。

---

## 7. 定存清單（高股息低波動 ETF：00713 式）

參考 5 檔高股息 ETF（00713 最像存股經理人）的指數規則。**季換股**。

### 7.0 這條線的真正用途：接手美元收益部位（v3.0 重定位）

**不是「再加一份收益」，是「換一個幣別拿同一份收益」。**

使用者現況（2026-09-07 盤點）：**75% 資產在美國特別股 + 債券 ETF + 銀行股**，
計畫**在未來一年內減碼、把約 10% 的總資產（~200 萬）轉回台股**，分批。

這改變三件事：

| | 一般「存股」 | 這條線 |
| :--- | :--- | :--- |
| **基準** | 0050 或大盤 | 🔴 **他賣掉的那些東西的稅後殖利率**。台股定存線對標不到，這個轉換就不該做 |
| **建倉方式** | 一次選 10 檔買下去 | **一年分批**——每季產出「這一季該建的部位」，季頻剛好，分批本身降低擇時風險 |
| **回撤容忍** | 跟股票組合一樣 | 🔴 **極低**。這筆錢從債券和特別股換過來，心理預期是「穩定收息」。第一年就 −20% 會讓他後悔整個轉換 → §7.1 那兩道新門檻（填息率 ≥ 60%、近 3 年含息報酬 ≥ 0）與低波動因子**不是可有可無，是核心** |

**稅的結構（要用你自己的級距算，這裡只給架構）**：美股股息對台灣居民預扣 30%
（台美無租稅協定），帳面 6% 稅後約 4.2%；台股股利可選併入綜所稅（抵減 8.5%、
上限 8 萬）或 28% 分離課稅，另加二代健保補充保費 2.11%（單筆給付 2 萬以上）。
中等級距下台股股利的稅後結果**可能**優於美股股息——**但這是結構不是答案，
實際數字請自己用扣繳憑單核對。本文件不構成稅務建議。**

### 7.0.1 殖利率門檻：**目標 5.5%、硬底線 5%**（使用者裁決 2026-09-07）

v1.0 的 `4%` 是隨手訂的，在 v3.0 的用途下太低——它要對標的是**你賣掉的美元部位**。

```
買入殖利率門檻 = max( 該股近 5 年平均殖利率 , 5.0% )      ← 硬底線
清單的目標水準  = 5.5% 左右
```

**為什麼 5.5% 對標得上**（結構，數字請用自己的級距重算）：

| | 帳面 | 稅後（粗估） |
| :--- | ---: | ---: |
| 美國特別股 | ~6% | **~4.2%**（預扣 30%，台美無租稅協定） |
| 台股 5.5%，邊際稅率 20% | 5.5% | **~4.7%**（併入綜所稅、抵減 8.5% 上限 8 萬，減二代健保 2.11%） |
| 台股 5.5%，選 28% 分離課稅 | 5.5% | **~3.8%** |

→ **中等級距下 5.5% 對標得上而且有餘裕；高級距（要選分離課稅）就未必。**
⚠️ 這是結構不是答案，**用你自己的扣繳憑單核對。本文件不構成稅務建議。**

🔴 **但 5% 硬底線的可行性要先驗**：台股大盤殖利率才 3–4%，
5% 疊上 §7.1 那七道硬門檻（連配 5 年無減配、FCF 覆蓋、填息率 ≥ 60%、
近 3 年含息報酬 ≥ 0…），**某些季度可能只剩 3–5 檔，組合建不起來**。
→ 實驗 B 的**第一個數字就是「每期候選檔數」**，不是報酬率。
建不起來就回報使用者調門檻，不要自己放水。

### 7.1 硬門檻（剔除）

- 近 4 季 EPS 每季為正
- 連續配息 ≥ 5 年、近 5 年無減配
- 近 3 年 FCF 覆蓋現金股利
- 現金股利主要來自盈餘（`earnings_div_ratio` ≥ 0.5）
- 負債比 ≤ 0.75（或產業中位數 × 1.5）
- **近 5 年平均填息率 ≥ 60%**（賺息賠價差＝假存股）
- **近 3 年含息年化報酬 ≥ 0**（NAV 在流血的高息＝陷阱）

### 7.2 存股安全分（過門檻的才排；rank 平均）

`FCF 殖利率(+) + 價格低波動(−vol) + ROE(+) + 填息率(+) + 配息穩定度(+) + payout_ratio(−)`
× **景氣循環懲罰**（近 12 季 rev_yoy／gross_margin 波動分位 → ×0.7～×1.0）

### 7.3 買入價（殖利率法）

```
買入殖利率門檻 = max( 該股近 5 年平均殖利率 , 5.0% )   ← v3.0：4% → 5%（§7.0.1）
估值買價       = 近 3 年平均現金股利 ÷ 買入殖利率門檻
下界           = max(季線, 近 20 週前低)
上界           = min(估值買價, 現價)
買入區間       = [下界, 上界]  if 下界 <= 上界，否則同 §6.3 的倒置處理
```

### 7.4 verdict 與揭露

- 現價殖利率 ≥ 門檻 且過所有硬門檻 → **推薦**；否則 **觀望 / 不推薦（原因）**。
- **產業集中度**：成分股單一產業 ≤ 40%。
- **揭露欄**：當期殖利率、近 3 年平均殖利率、殖利率 vs 5 年區間、填息率、產業、本期換手率。

> §6/§7 的因子計算 tw-swing `twswing.value.factors/screen` 已寫好——M0 搬進 tw-hold、
> 改 native。填息率／含息報酬是新增計算，M2 補（要 `prices_raw_close` + `dividend.ex_date`）。

---

## 8. 技術棧 · repo · 佈署

- Python 3.14、Streamlit、**plotly**、pandas、pyarrow、requests。
- repo（**同一個 GitHub 帳號，不開新帳號**，§9 已定 10）：

| repo | 公開性 | 內容 |
| :--- | :--- | :--- |
| **`tw-data`** | **公開** | 抓取 + 打包 + 發佈 bundle。Actions 免費無上限 |
| **`tw-swing`** | 私有 | 短波段交易系統（既有，第一期不動） |
| **`tw-hold`** | **私有** | 本專案。清單、持倉、個股頁都不外流 |

- **M0 第一步**：建 `tw-data`（公開）＋ 推 `tw-hold`（私有）。
- ⚠️ **公開的是市場資料，不是你的決策**：bundle 公開；三清單、模擬單、
  `positions.json` 全在私有 repo。這兩件事不要混。
- **佈署：Streamlit Community Cloud**（v1）
  - 免費、GitHub 直連（**支援 private repo**）、`st.secrets` **只放 app 密碼**。
  - ✅ **雲端不需要任何 token**：bundle 走公開 Release 匿名下載；
    `FINMIND_TOKEN` 只在本地 `.env`（個股即時補抓在雲端是停用的，§3.3）。
    **雲端零密鑰 = 零外洩面。**
  - 取捨：閒置會睡（冷啟 ~30s）、1GB RAM、公開 URL（app 內密碼擋）、無常駐磁碟。
  - 三清單只渲染 `data/derived/`，運算量趨近 0 → 這些限制都無感。
  - ⚠️ **個股頁不一樣**：它要執行期拉 bundle（§3.2），1GB RAM 是**真的會撞到的**上限
    → 按需讀取（pyarrow row/column filter），且 `st.cache_resource` 只快取
    「已載入的那幾檔」不是整張表。
  - **個股即時補抓 = 本地進階模式**，雲端停用（偵測環境變數）。
- **週更財報 = tw-swing 的 `fundamentals.yml`**；**每日重算三清單 = tw-hold 自己的 Action**。
- 不要 gunicorn（Streamlit 內建）。

---

## 9. 決定與待定（2026-09-07 二次修正）

**已定**：
1. 資料（**v2.2**）：獨立的 **`tw-data`（公開 repo）** 是唯一的抓取者，
   發佈 bundle 到**自己的公開 Release**（tag `data-latest`），**下游匿名下載、零 token**；
   分 **U1a（週更）/ U1b（日更）** 兩條管線，兩條都住 tw-data（§3.1.1）；
   tw-hold `fetch_bundle.py` 拉，且**只有 U1a 資產時也要能跑**；
   共用碼複製（`loader.py` 是搬移，tw-swing 端刪除）不 import；
   tw-hold 每日 Action 重算三清單 commit 回自己（私有）repo。
   🔴 **紅線：第一期不修改 tw-swing 的 `daily.yml`**（G-5 時鐘）。
2. 佈署：**Streamlit Community Cloud**；即時補抓個股僅本地。
3. 評估法分軌：長波段主動（§5，含寫死出場規則）；價值/定存規則化因子指數（§6/§7，季換股）。
4. **刪** forward EPS × 市況 PE 目標價機器。價值買價＝「你買進的殖利率（主）+ 自身歷史 PE
   分位帶（明細）」（使用者裁決：兩個都給）。`regime` 只當顯示旗標。
   **估值上緣不進 verdict**（v2.1 修正，§6.3）。
5. 定存新增門檻：填息率 ≥ 60%、近 3 年含息報酬 ≥ 0；成分產業 ≤ 40%。
6. `per` 整併成一張表——tw-swing publish step 負責。
7. F-Score：9 分項打勾、不加總、不當 verdict。
8. **不開新 GitHub / FinMind 帳號。**
8.5 **波段軌道 = 主動選股候選池**（v3.1）：狀態型條件（CANSLIM + 趨勢模板）、
   **不給 verdict／不給買價／不合成總分／強制並列反對證據**、買賣由人決定。
   代價是**不宣稱任何報酬**，因此不需要也不准走回測門檻。pool3 已否決（§5.0）。
9. **公開性（v2.2 定案）**：`tw-data` 公開、`tw-swing` 與 `tw-hold` **私有**。
   公開的是市場資料，不是決策產出（§8）。
10. **不開新 GitHub 帳號**（也不開新 FinMind 帳號）。額度問題已由「tw-data 公開 →
   Actions 免費無上限」解決，不需要第二個帳號；且 GitHub 條款規定一個自然人
   只能有一個免費帳號，為繞開用量上限開第二個，最壞情況是**兩個帳號一起被停**
   ——連正在跑的 G-5 管線都會沒有。
11. **tw-swing 繼續維護，不放生。** 「讓它跑但不維護」等於放棄真錢目標：G-5 要的是
   連續 60 交易日無中斷，而 LOST 級中斷需要**人**手動補跑（實測過上游延遲 13.7h、
   scheduler 延遲 9–12h 的日子），無人看管必斷。

**🔴 v3.0 之後「已定」全部降級為「假設」，等實驗 B 的回溯結果。** 上面那幾條講的是
架構與工程（仍然成立）；**因子設計、門檻數字、清單長度一律未定案。**

**待定（v3.0 新增，按重要性）**：
- 🔴 **價值/定存的因子在台股前 500 大到底有沒有超額報酬** → 實驗 B（`docs/BACKTEST_HANDOFF.md`）。
  這條沒答案之前不要寫 UI、不要接 bundle 管線。
- ~~定存線的殖利率門檻~~ → **已裁決：目標 5.5%、硬底線 5%**（§7.0.1）。
  🔴 **但可行性未驗**——實驗 B 要先報「每期候選檔數」，建不起來就回頭調。
- 🔴 **買入指引區間**：三條線都要有（使用者要求 2026-09-07）。
  價值/定存已有（§6.3/§7.3）；**pool3 的公式在 tw-swing 那邊，見
  `docs/BACKTEST_HANDOFF.md` A-2c**，未定案。
- **資金配置**：波段 ~200 萬（有實證）／價值 ~100 萬（無實證，先小）／定存 ~200 萬
  （一年分批）→ 回溯完再定案。
- 長波段移出後，`reference/` 還要不要 `indicators/`（只剩均線需求）→ M0 確認。
- ~~`tw-hold-data` 獨立公開 repo vs Release + PAT~~ → **v2.1 定案：Release + PAT**（§3.1）。
- 市值口徑：`收盤價 × capital_stock/10` vs 暫用 `PBR × 股東權益` → 看 `capital_stock`
  補抓修不修得好，M0a 定（§3.1）。
- 產業別資料來源（`universe.parquet` 要帶）：FinMind `TaiwanStockInfo` 的 `industry_category` → M0 確認欄位。
- 長波段清單重算頻率：v1 每日（Action）；個股出場靠使用者自己看 → 加推播 → v2。
- 季換股「公告後」對齊哪一天 → M2 定。
- 產業逆風判定（§5.2 財務惡化的一部分）：用同產業其他股票動能中位數當代理 → M2 定。

---

## 10. 失敗模式與守門員（opus 審核 2026-09-07 追加）

> **這一節的存在理由**：三清單算錯不會拋錯，管線斷掉不會有人來抗議。
> 拆成兩個 repo 之後，**跨 repo 的錯全是靜默的**。每一條都配一個「會叫的東西」。

### 10.1 額度：GitHub Actions 分鐘數是**帳號級共用**的

🔴 **這是整條路上最貴的單一風險**，而且它咬的是 tw-swing 不是 tw-hold。

**額度是「帳號級」共用的，不是每個 repo 各自 2000 分。**

| | 分鐘/月（估） | 公開 tw-hold 之後 |
| :--- | ---: | ---: |
| tw-swing `daily.yml` 三槍（現況） | ~990 | ~990 |
| tw-swing `fundamentals.yml`（現況） | ~100 | ~100 |
| **新增** `publish_bundle.yml`（U1b，每日抓+打包） | ~330 | **~330** |
| **新增** `per` 抓取進 CI（U2，每週 500 requests） | ~60 | **~60** |
| **新增** tw-hold `rebuild.yml`（每日重算+commit） | ~100 | **0**（公開 repo 不計） |
| **合計** | ~1580 | **~1480** |

**✅ 使用者裁決 2026-09-07：tw-hold repo 設 public。**

- 公開 repo 的 Actions **不計入帳號額度** → 省掉 tw-hold 自己那 ~100 分。
- ⚠️ **但大頭沒有被省掉**：`publish_bundle.yml`（~330）與 `per`（~60）住在
  **tw-swing，而 tw-swing 維持私有** → 仍然計費。
  1480 / 自訂上限 1600 → **餘裕只剩 ~120 分/月**。
- **超過 2000 = 私有 repo 的 Actions 直接停跑** → `daily.yml` 斷 → **G-5 的 60 交易日
  時鐘歸零、跳到明年 2 月**。
- **所以下面三條照做，一條都不能省**（公開 repo 只解決了四分之一）：
  1. 🔴 **`publish_bundle.yml` 用 `actions/cache` 快取上游資料包（220MB）**——
     這才是那 330 分的主要來源，是真正有效的那一刀。
  2. tw-hold 的 `rebuild.yml` 改由 tw-swing publish 完成後 `repository_dispatch` 觸發，
     **非交易日不空跑**（額度上已免費，但可省冷啟與無意義的 commit）。
  3. **額度看門狗**：每月 1 號查一次 Actions 用量，> 80% 就告警。

**公開之後多出來的兩件事**：

- **PAT 要收窄**：tw-hold 是公開 repo，它的 Actions secrets 雖然不會被 fork 取得，
  但假設外洩仍要讓損失有界 → 用 **fine-grained PAT，權限只給 tw-swing 一個 repo 的
  `contents: read`**，不要用 classic PAT 的全帳號 `repo` scope。到期日記進
  `reference/UPSTREAM.md`（守門員 G6）。
- **公開的是清單、不是 bundle**：`data/derived/` 三張清單公開；FinMind 衍生的整包
  bundle 仍在 tw-swing 私有 Release（§3.1 拒絕做「免費 FinMind 鏡像」的理由不變）。

### 10.2 守門員清單（每條都要有「會叫的東西」）

| # | 靜默失敗 | 守門員 |
| :- | :--- | :--- |
| **G1** | **schema 漂移**：tw-swing 改欄位名/型別，tw-hold 隔天算出錯的數字 | `_meta.json` 帶 `schema_version` + 每檔欄位清單；`fetch_bundle.py` 開場 assert，**不符就 fail**，不要「盡力而為」 |
| **G2** | 🔴 **還原 / 未還原搞混**：`prices_raw_close` 若被誤填成還原序列，**填息率恆等於 100%、定存清單全綠、沒有任何東西會報錯** | 一條 assertion test：抽 `0056` / `2412` 的除息日，檢查 **raw 序列有跳空、adj 序列沒有**（tw-swing PRD §5.2.1 實測 0056 除息日 +0.36%～+1.14%）。跳空不見了就 fail |
| **G3** | **bundle 是舊的**：tw-hold 排在 tw-swing publish 之前跑，吃到昨天的資料 | `_meta.json` 帶 `trading_date`；tw-hold 比對「預期最新交易日」，不符**不是失敗、是在清單頁標記資料日期** |
| **G4** | **tw-hold 的 Action 靜默斷掉**：連續失敗沒人看，60 天後 GitHub 自動停用排程 | 抄 `daily.yml` 的 `heartbeat` + `notify-failure` job（⚠️ 告警 job 自己絕對不可以失敗） |
| **G5** | **複製碼漂移**：`regime` / `indicators` / `finmind_client` 上游改了、這邊沒跟 | `reference/UPSTREAM.md` 記「檔案 / 來源路徑 / commit」＋ `check_upstream_drift.py` 比 hash（**只提醒，不自動同步**） |
| **G6** | **PAT 過期**：fine-grained PAT 最長 1 年，過期後 bundle 拉不到 | 到期日寫進 `UPSTREAM.md` 與 `_meta.json` 的註記；G4 的告警會先叫 |

### 10.3 `prices_raw_close` 的資料來源（M2 之前必須定，否則填息率做不出來）

🔴 **這條現在完全沒有人在抓，而且它是定存兩道新硬門檻之一的原料。**

已查證的事實（tw-swing `data/finmind.py` + `docs/DATA_INVENTORY.md`）：

- `TaiwanStockPriceAdj`（還原價）**免費層拿不到**（實測 HTTP 400「Your level is free」）
  → tw-swing 的還原序列來自**上游資料包**，不是 FinMind。
- `TaiwanStockPrice`（**未還原** OHLCV）**是免費的**——所以原料拿得到。
- 但 FinMind **沒有整批端點**：500 檔 × 5 年 = 500 requests 一次性，
  之後要維持就是**每天 500 requests**（500/hr 天花板 → 每天一小時純抓取，還會跟
  `fundamentals.yml` 撞）。**不可接受。**

**定案（v2.1）**：
1. **歷史一次性回補**：本地用 FinMind `TaiwanStockPrice` 抓 500 檔 × 5 年（500 requests，
   一個下午），落成 `prices_raw_close.parquet` 的基底。
2. **之後每日增量**：改用 **TWSE/TPEx OpenAPI 的每日收盤整批檔**（1 request/日，
   tw-swing 抓處置股／月營收已經在用同一類端點）→ 併進 U1b。
3. ⚠️ 這條沒定就開工，會在 M2 寫填息率時才撞到，那時程式已經寫一半。

### 10.4 已查證安全、但要記下來的一條

**擴大 `universe.parquet` 的宇宙（市值 ∪ 成交值）不會反咬 tw-swing 的 live 訊號。** 查證：
- `pe_percentile` 是**自身 756 交易日滾動百分位**（`export_field_catalog.py`），
  不是橫斷面 rank → 多抓幾檔不會改變既有標的的數值。
- tw-swing 的掃描宇宙是獨立的 `UNIVERSE_TOP_N = 500` **流動性排名**
  （`screener/precompute.py`）→ 多抓的那些安靜大型股落在掃描宇宙**之外**。

→ 結論：安全。**但這件事必須寫下來**，否則下一個人要嘛白擔心一輪、要嘛反過來
「順手」把掃描宇宙也一起改了——那才是真的會動到 live。

---

## 11. 變更記錄

- **2026-09-07 v3.1-draft 波段軌道復活（未定案）**——起因：pool3 的回測結果回來了，
  把 v3.0 砍掉這一軌的理由推翻了一半。
  1. **pool3 回測否決**（§5.0）：A-1 沒有任何長出場變體同時過四門檻與樣本外；
     A-2 延後 +2 日樣本外翻負；連 +1 日那格也只有 1.3 個標準誤。
     Y4 年化 +16.88% 還輸 0050 的 +24.12%。
  2. **真正的結論**：在這個地基上「訊號有超額報酬」和「一週看一次」是衝突的。
     事件式技術訊號的超額集中在訊號後兩週，這是本質不是缺陷。
  3. **復活的不是原本那個東西**：放棄「訊號有超額報酬」的宣稱，換取「一週看一次」。
     條件從**事件**改成**狀態**（CANSLIM + Minervini 趨勢模板），
     產出從**推薦清單**改成**候選池**，買賣由人決定（使用者裁決：「要不要買我自己決定」）。
  4. **四條守則**（§5.1）防止它變回 `me`：不給 verdict、不合成總分、
     強制並列反對證據、必須給失效條件。
  5. **價格給「風險多大」不給「值多少」**（§5.3）：停損參考位 / 風險% / 可買上限
     由風控推導，**明白標示沒有回測支撐**。
  6. bundle 新增 `chips.parquet`（法人買賣超，§5.2.1 要用）。
  7. 新增前置：**實驗 D**（每週候選檔數普查）與**實驗 C**（pool1/2 的進場口徑對照，
     公平性問題）→ `docs/BACKTEST_HANDOFF.md`。

- **2026-09-07 v3.0-draft 範圍重定（未定案）**——起因：使用者盤點自身資產與使用節奏
  後的一場長討論。四個改判：
  1. **長波段移出到 tw-swing pool3**（§5）。查 `BENCH.md` 發現 Y 系六條全部通過
     四門檻 + 樣本外，Y4（軌道突破 + 月營收加速 + PE 未過熱 = CANSLIM）每筆超額
     +1.773%、MAR 2.34、回撤 −7.2%——**使用者想要的「成長性選股波段」已經被驗證過**，
     卡的是工程（CI 沒 FinMind 資料）不是效果。在 tw-hold 重做一個沒風控沒驗證的
     版本是同一件事做兩次、且做得比較差。
  2. **定存線重定位**（§7.0）：從「存股」改成「**接手美元收益部位**」——使用者
     75% 資產在美國特別股/債券 ETF/銀行股，要在一年內轉 10%（~200 萬）回台股。
     基準、建倉方式、回撤容忍全部跟著改。
  3. 🔴 **「不上真錢所以豁免回測」的前提消失**。實際投入金額（定存 200 萬 + 價值
     100 萬）比波段線還大。→ 新增前置實驗 B（組合層歷史回溯），
     見 [`docs/BACKTEST_HANDOFF.md`](docs/BACKTEST_HANDOFF.md)。
  4. **日推薦 + 週推薦並存**：pool1/pool2 維持日頻（零用錢、想玩才開），
     pool3 走週批次（主力）。「訊號頻率 ≠ 決策頻率」。
  - 移出長波段帶來的簡化：不用 `pivots`/`trendlines`/`positions.json`/`index_0050`/
    `regime`，每日重算改月/季頻（§5.2）。
  - ⚠️ **本版未定案**：因子設計與門檻數字全部視為假設，等實驗 B。

- **2026-09-07 v1.0 定案**：架構定為「tw-swing 上游、資料層共用」。§6 用投信經理人視角
  （forward EPS × 市況 PE → 目標價 → 安全邊際 → verdict）。本地跑 Streamlit。
- **2026-09-07 v2.0 二次修正**（同日晚）：
  - **抓資料**：本機磁碟讀取 + `import twswing.*` → HTTP 拉 bundle + 複製共用碼。
  - **佈署**：本地 → Streamlit Community Cloud + 本地進階模式。
  - **評估法**：三清單共用「基金分析紀律」（§2A）。組合建構分軌——定存/價值＝規則化
    因子指數（季換股），**長波段維持主動**（§5.3 進場前寫死出場規則）。
  - **刪** v1.0 目標價機器。價值改「你買進的殖利率（主）+ 自身歷史 trailing PE 30/70
    分位帶（明細）」。`regime` 降為顯示旗標。
  - 定存 §7 補「填息率」「含息報酬」兩道門檻 + 產業集中度上限。
  - 起因：使用者關機後反思——(a) 本機路徑依賴上不了網；(b) 把長波段也塞進季換股
    被動框架是類別錯誤，長波段本質是主動擇時交易。
  - 介面草模：`scratchpad/tw-hold-mock.html`（四分頁 + 買價兩方案對照）。
- **2026-09-07 v2.1 opus 審核修正**（`docs/REVIEW_REQUEST.md` 的回覆；審核結論
  「需修正後可動工」，修正 8 條已全部套用，其餘設計維持不變）：
  1. **U1 拆 U1a / U1b**（§3.1.1）——M0 原本卡在「publish step 要動 `daily.yml`」而
     `daily.yml` 鎖在 G-5 時鐘上。拆完 M0a 現在就能開工。
  2. **bundle 改走 tw-swing 私有 repo 的 Release + PAT**，不建公開 `tw-hold-data`（§3.1）。
  3. **市值資料源列為 M0 硬前置**——`twswing.data.universe` 沒有市值/產業，
     `capital_stock` 還在補抓當掉（§3.1）。
  4. **`loader.py` 改為搬移**（tw-swing 端刪除），不留兩份（§3.2）。
  5. **估值上緣退出 verdict**（§6.3）＋ 循環高峰顯示旗標（§6.2）。
  6. **買入區間加倒置處理**（§6.3、§7.3）；刪空條件「單檔 ≤ 30%」（§6.1）。
  7. **長波段加 `positions.json` 持倉登錄**（§5.3 出場檢查表沒它算不出來）
     ＋ 措辭改「條件成立區間」＋ v1.5 補跑一次回測（§5）。
  8. **「新進/移除」明確讀上一期 `lists/*.json`**，不靠 git diff（§3.2）。
